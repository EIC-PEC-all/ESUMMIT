# ─────────────────────────────────────────────────────────────
# Monorepo: ESUMMIT (public site) · E_Summit_Backend (API) · esummit-admin (portal)
# Patterns are unanchored so they apply at every depth, not just the repo root.
# ─────────────────────────────────────────────────────────────

# dependencies
node_modules/
.pnp
.pnp.js

# testing
coverage/

# next.js
.next/
out/

# build output
build/
dist/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# local env files — never commit real secrets (.env.example IS committed)
.env
.env.*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# os
.DS_Store
